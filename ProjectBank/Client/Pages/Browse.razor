@page "/browse"
@page "/" 
@* @page "/" burde redirect til page "/browse"* *@


<PageTitle>ProjectBank - Browse</PageTitle>

    @if (keywords == null)
    {
        
    }else{
        <div class="grid-container">
            @foreach (var keyword in keywords)
            {
                <button class="grid-item" onclick="location.href='/swipe'"> @keyword</button>
            } 
        </div>
    }

@code{
    private string[]? keywords = {"AI", "Software", "Database", "Security",
         "Machine Learning", "API", "DotNet", "Keyword1", "Keyword2", "Keyword3", 
         "Keyword4", "Keyword5", "Keyword6", "Keyword7", "Keyword8", "Keyword9"};
}
